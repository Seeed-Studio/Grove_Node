<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.seeedstudio.ble.node.MapActivity$PlaceholderFragment" >

    <ListView
        android:id="@+id/ifff_list_view"
        android:layout_width="match_parent"
        android:layout_height="128dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" >
    </ListView>

    <Button
        android:id="@+id/add_ifff_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:text="Add" />

    <TextView
        android:id="@+id/then_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/add_ifff_button"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="32dp"
        android:text="then"
        android:textAppearance="?android:attr/textAppearanceLarge" />

    <Spinner
        android:id="@+id/event_spinner"
        android:layout_width="64dp"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/then_text_view"
        android:layout_marginRight="16dp"
        android:layout_toLeftOf="@+id/then_text_view" />

    <TextView
        android:id="@+id/if_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/event_spinner"
        android:layout_alignBottom="@+id/event_spinner"
        android:layout_marginRight="16dp"
        android:layout_toLeftOf="@+id/event_text_view"
        android:text="if"
        android:textAppearance="?android:attr/textAppearanceLarge" />

    <Spinner
        android:id="@+id/action_spinner"
        android:layout_width="64dp"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/then_text_view"
        android:layout_marginLeft="16dp"
        android:layout_toRightOf="@+id/then_text_view" />

</RelativeLayout>